name: GH workflow 1
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
